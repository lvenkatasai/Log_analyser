# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
env.bak/
venv.bak/

# Flask stuff
instance/
.webassets-cache
uploads/

# IDEs
.vscode/
.idea/

# Local logs
# large_sample_logs.log is quite large, so let's prevent any future large files from slipping in accidentally
# However, we'll allow standard sample logs if needed.
# Gemini artifacts (implementation plans and walkthroughs) are handled externally but we can explicitly ignore them if ever placed here.
implementation_plan.md
walkthrough.md
task.md
.gemini/
